-- for every table
-- get a count(*) and a count(distinct [pk])
-- if these don't match, chekc max date, then try using a date range to get a perfect match

-- run reports queries for users and programs and compare